<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Anniversary_Count__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    AND(
        NOT(ISNULL(Joining_Date__c)), 
        Joining_Date__c &lt;= TODAY()
    ),
    YEAR(TODAY()) - YEAR(Joining_Date__c) - 
    IF(
        DATE(YEAR(TODAY()), MONTH(Joining_Date__c), DAY(Joining_Date__c)) &gt; TODAY(),
        1,
        0
    ),
    0
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Anniversary Count</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
